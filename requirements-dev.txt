# Librarian dev dependencies — pinned on Python 3.12.10.
# Librarian aims to stay dependency-light for portability. PyYAML may be added in
# Phase 1 for frontmatter emission, or hand-rolled to keep the writer zero-dep.
pytest==9.0.3
