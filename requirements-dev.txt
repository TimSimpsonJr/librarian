# Librarian dev dependencies — pinned on Python 3.12.10.
# Installs the runtime deps plus the test tooling. PyYAML lives in
# requirements.txt because scripts/write_note.py imports it at runtime.
-r requirements.txt
pytest==9.0.3
